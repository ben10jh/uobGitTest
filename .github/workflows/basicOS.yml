name: Myworkflow file
on:
  push:
    branches: [main]

Jobs:
  builds:
  #create VM with ubuntu OS
  runs-on: Ubuntu-20.04
  name: My Build
  
  steps:
  #first step to say hello
    -name: greeting steps
    runs: 
      echo "Hello there"
